import 'package:flutter/material.dart';

class FindAcupointPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
			appBar: AppBar(
				title: Text("找穴找方"),
			),
			body: Center(
				child: Text("找穴找方"),
			),
		);
  }
}
